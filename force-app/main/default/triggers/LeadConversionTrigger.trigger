trigger LeadConversionTrigger on Lead (after update) {
 /* List<Lead>leadsnew=Trigger.new;
   List<Lead>leadsconvert=new List<Lead>();
    for(Lead l:leadsnew){
        if(l.IsConverted==true){
            leadsconvert.add(l);
        }
    }
    LeadConversionClass.method(leadsconvert); */
}